import { StyleSheet, View } from "react-native";

function GameOverScreen({ userNumber }) {
  return <View style={styles.gameOverScreen}></View>;
}

export default GameOverScreen;

const styles = StyleSheet.create({
  gameOverScreen: {},
});
